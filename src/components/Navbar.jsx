const Navbar = () => {
  return (
    <div className="navbar">
      <a href="#" className="title">Gilad.dev</a>
      <div className="right">
        <a href="#">Home</a>
        <a href="#about">About</a>
        <a href="#projects">Projects</a>
        <a href="#contact">Contact</a>
      </div>
    </div>
  );
};

export default Navbar;
